# UID/GID de l'utilisateur hôte — copier ce fichier en .env et adapter si besoin.
# Sur Linux/WSL2 : récupérer avec `id -u` et `id -g`
# Sur macOS     : généralement 501 / 20
SYGMA_UID=1000
SYGMA_GID=1000
